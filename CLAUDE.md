# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build

This is a Maven project targeting Java 21. Common commands:

```bash
mvn compile          # compile
mvn test             # run tests
mvn test -Dtest=Xxx  # run a single test class
mvn package          # package
mvn clean            # clean build output
```

Maven wrapper (`mvnw`) is not yet set up — use the system `mvn` command.

## Project

- Group: `com.tianji`, artifact: `skillMatch`, version `1.0-SNAPSHOT`
- The project was freshly initialized and has no source code, modules, or dependencies yet.

不要改后端代码!!!后端只提建议
